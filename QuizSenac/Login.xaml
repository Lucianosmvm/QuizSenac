<Page x:Class="QuizSenac.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Login">

    <Grid Background="White">
        <!-- Fundo -->
        <Image Source="/Quiz Senac 2025 (8).png"
               Stretch="UniformToFill"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"/>

        <!-- Card centralizado -->
        <Border Width="700" Height="650"
                Background="White"
                CornerRadius="10"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="Black"
                                  BlurRadius="20"
                                  ShadowDepth="5"
                                  Opacity="0.4"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="LOGIN"
                           FontSize="72"
                           FontWeight="Bold"
                           Foreground="#FF123D70"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <Label
            HorizontalAlignment="Center" FontSize="36" FontWeight="Bold" Foreground="#FF123D70" Content="Nome:"/>

                <TextBox
            Width="411" x:Name="txb_nome" Margin="0,40,0,25" Height="51" FontSize="18" BorderThickness="2" BorderBrush="#FFF09C20"/>
                <Button
                        Width="228"
                        Height="114"
                        x:Name="btn_login" FontSize="36" FontWeight="Bold" Foreground="#FF123D70" Content="Entrar" BorderBrush="White" Click="btn_login_Click_1">
                    <Button.Background
            >
                        <LinearGradientBrush
            EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop
            Color="White" Offset="0.1"/>
                            <GradientStop
            Color="#FFFF9100" Offset="0.8"/>
                        </LinearGradientBrush>
                    </Button.Background>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Page>
